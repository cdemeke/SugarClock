# Upstash Redis (Vercel KV) REST credentials.
# In Vercel, create an Upstash Redis (KV) integration and these are injected
# automatically. For local dev, copy this file to .env.local and fill in.
KV_REST_API_URL=
KV_REST_API_TOKEN=
